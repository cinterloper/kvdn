package net.iowntheinter.kvdn.storage.meta.types

class UserDefinedFunction {

    /*
    a user defined function is a Class or script (if scriptengines are installed and loaded)
    It is passed a refrence to the object its called on (self) and any arguments
     */
}
